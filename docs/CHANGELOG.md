# Unreleased

- Internal initialization fix: minor update in `src/__init__.py` to ensure the package initializes correctly when the application is loaded. This is an implementation detail and does not introduce user-facing changes or alter API behavior. No action required from users.
